<div class='container'>
  <div class='row'>
    <div class='col-md-3'>
      <!--<h2>User info</h2>-->
      <!--</%= render 'users/info', user: @book.user %>-->-->
      <h2 class="mt-3">New book</h2>
      <%= render 'new', post_image: PostImage.new %>
    </div>
    <div class='col-md-8 offset-md-1'>
  		<h2>shop detail</h2>
  		<table class='table'>
  		  <tr>
          <td><%= link_to(@post_image.user) do %>
            <%= @post_image.user.name %>
            <% end %>
          </td>
          <td><%= link_to @post_image.shop_name, @post_image %></td>
          <td><%= @post_image.body %></td>
          <!--<td class=</%= "favorite-" + @book.id.to_s %> >-->
            <!--<?%= render "favorites/btn", book: @book %>-->
          <!--</td>-->
          <!--<td class="book-comments-counter">-->
            <!--</%= render "book_comments/counter", book: @book %>-->
          <!--</td>-->
          <% if @post_image.user == current_user %>
            <td><%= link_to 'Edit', edit_post_image_path(@post_image), class: "btn btn-sm btn-success edit_post_image_#{@post_image.id}" %></td>
            <td><%= link_to 'Destroy', @post_image, method: :delete, data: { confirm: '本当に消しますか？' }, class: "btn btn-sm btn-danger destroy_post_image_#{@post_image.id}" %></td>
          <% end %>
        </tr>
      </table>

      <!--<div class="book-comments-index">
        </%= render "book_comments/index", book: @book %>
      </div>

      </%= render "book_comments/form", book: @book, book_comment: @book_comment %>-->

    </div>
  </div>
</div>